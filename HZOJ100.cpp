#include <iostream>
#include <cstdio>
using namespace std;

int main() {
    int n;
    cin >> n;
    double m = 0;
    m = (m + n) * 1.00417;
    m = (m + n) * 1.00417;
    m = (m + n) * 1.00417;
    m = (m + n) * 1.00417;
    m = (m + n) * 1.00417;
    m = (m + n) * 1.00417;
    printf("$%.2lf\n", m);
    return 0;
}
